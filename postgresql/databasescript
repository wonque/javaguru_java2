CREATE SCHEMA IF NOT EXISTS java2 AUTHORIZATION atols;

DROP TABLE IF EXISTS java2.products;

CREATE TABLE IF NOT EXISTS java2.products (
	ID BIGSERIAL PRIMARY KEY,
	TITLE VARCHAR(40) NOT NULL,
	DESCRIPTION VARCHAR(140),
	CATEGORY VARCHAR(40),
	PRICE DECIMAL(10, 2)
);

